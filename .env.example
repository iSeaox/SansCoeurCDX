# Variables de configuration
# Copiez ce fichier en ".env" et ajustez les valeurs.

# Clé secrète Flask (changez en production)
SECRET_KEY=change-me

# Chemin de la base SQLite (par défaut: data/coinche.db dans le projet)
#DATABASE=/chemin/absolu/coinche.db

# Paramètres serveur
# Adresse d'écoute (0.0.0.0 pour toutes interfaces)
HOST=0.0.0.0
# Port d'écoute
PORT=5000
# Mode debug (true/false, 1/0, yes/no, on/off)
DEBUG=true

# Si vous utilisez "flask run", ces variables sont prises en compte par le CLI
# (Flask charge automatiquement .env si python-dotenv est installé)
FLASK_RUN_HOST=0.0.0.0
FLASK_RUN_PORT=5000
FLASK_DEBUG=1

# ----- SMTP (emails) -----
# Hôte SMTP (obligatoire pour envoyer des emails)
SMTP_HOST=smtp.exemple.com
# Port SMTP (465 pour SSL direct, 587 pour STARTTLS)
#SMTP_PORT=587

# Identifiants (si requis par votre serveur SMTP)
SMTP_USER=votre.utilisateur@exemple.com
SMTP_PASSWORD=votre_mot_de_passe

# Adresse d'expéditeur (utilise SMTP_USER si non défini)
#SMTP_FROM=

# Sécurité
# Utiliser une connexion SSL directe (souvent port 465)
SMTP_USE_SSL=false
# Utiliser STARTTLS (recommandé, souvent port 587)
SMTP_USE_TLS=true

# Délai d'attente en secondes
SMTP_TIMEOUT=10

# GOOGLE
RECAPTCHA_SITE_KEY=site_key
RECAPTCHA_SECRET_KEY=secret_key
RECAPTCHA_ID=id
